# 레인 C(Colab 실전 점검)를 마지막으로 끝낸 날.
#
# 레인 A·B 는 cron 이 돌려 주지만 레인 C 는 사람이 Colab 에서 손으로 돈다.
# 그래서 "마지막이 언제였는가"를 기억해 줄 곳이 코드 안에 없다.
# 이 한 줄이 그 자리다.
#
# 쓰는 법:
#   레인 C 를 끝낸 날 아래 날짜를 오늘 날짜로 고치고 커밋한다. 그것뿐이다.
#
# 이 날짜를 읽는 곳 둘:
#   1) tests/update_site.py   — index.html 의 '실환경 점검' 칸에 찍는다(매주).
#                               100일이 넘으면 주간 실행 로그에 경고를 낸다.
#   2) .github/workflows/lane-c-reminder.yml
#                             — 분기마다 깨어나, 80일이 넘었으면 이슈를 연다.
#
# ⚠ 고치는 것을 잊어도 조용해지지 않는다. 날짜가 그대로면 경과일이 계속 늘어
#   경고가 커진다. 틀리더라도 '아직 안 했다' 쪽으로 틀리므로 안전하다.
#
# 형식: YYYY-MM-DD 한 줄. # 로 시작하는 줄과 빈 줄은 무시한다.

2026-08-22
